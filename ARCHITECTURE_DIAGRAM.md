# Territory System Architecture Diagram

## System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER RUNS GPS ACTIVITY                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Frontend: ActiveRun.tsx                                         │
│  • Collects GPS points every 2-5 seconds                        │
│  • Stores: {lat, lng, timestamp, accuracy}                      │
│  • Validates points (anti-cheat checks)                         │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  POST /api/runs                                                  │
│  • Body: { points: [], distanceKm, durationSec, activityType }  │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Backend: runRoutes.js - POST /                                 │
│  1. validateRunSubmission() - check points                       │
│  2. lineString(points) - create line from GPS                    │
│  3. turfBuffer(line, 0.05km) - create ±50m buffer               │
│  4. Pool transaction BEGIN                                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
    ┌──────────────┐      ┌──────────────┐
    │ INSERT run   │      │ INSERT       │
    │ + geojson    │      │ territory    │
    │ + distance   │      │ from buffer  │
    └──────────────┘      └──────────────┘
          │                     │
          └──────────┬──────────┘
                     ▼
    ┌──────────────────────────────┐
    │ UPDATE user_stats            │
    │ • total_distance_km += dist  │
    │ • territories_owned += 1     │
    │ • area_km2 += dist           │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │ Pool transaction COMMIT      │
    │ Cache invalidation           │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │ Response:                    │
    │ {                            │
    │   ok: true,                  │
    │   runId: 123,                │
    │   territoryCreated: true     │
    │ }                            │
    └──────────────┬───────────────┘
                   │
                   ▼
         ┌─────────────────────┐
         │ Frontend receives   │
         │ Shows "Territory    │
         │ Created! 5km 🏆"    │
         └─────────────────────┘
```

## Database Relations

```
┌─────────────────────────┐
│      users              │
├─────────────────────────┤
│ id (PK)                 │
│ email                   │
│ username                │
│ avatar_url              │
└──────────┬──────────────┘
           │ 1:N
           │
           ▼
┌─────────────────────────┐         ┌──────────────────────┐
│      runs               │────────>│   territories        │
├─────────────────────────┤  1:1    ├──────────────────────┤
│ id (PK)                 │         │ id (PK)              │
│ user_id (FK)            │         │ run_id (FK) [UNIQUE] │
│ geojson [LineString]    │         │ owner_id (FK)        │
│ distance_km             │         │ geojson [Polygon]    │
│ duration_sec            │         │ distance_km          │
│ activity_type           │         │ activity_type        │
│ created_at              │         │ created_at           │
└─────────────────────────┘         └──────────────────────┘
                                           │
                                           │ N:1 (User owns)
                                           │
                                           └──────────┐
                                                      │
                                    (back to users)──┘
```

## Territory Creation Sequence

```
GPS Points                 LineString               Buffered Polygon
───────────────           ──────────────           ─────────────────

(40.1, -74.1)                                        ╱──────╲
      •                      ●                      ╱        ╲
            │             ╱                        ╱          ╲
(40.2, -74.2)│         ●─────                      │  Buffer  │
      •      │        ╱                            │   ±50m   │
            ╱      ●                               ╲          ╱
(40.3, -74.3)   ╱                                   ╲        ╱
      •      ●─────                                 ╲──────╱
            │
(40.4, -74.4)
      •

   Step 1:                Step 2:                  Step 3:
   Raw GPS Points         Create Line              Add Buffer
```

## Territory Features Comparison

```
┌─────────────────────────────────────────────────────────┐
│                  TERRITORY PROPERTIES                     │
├──────────────────────┬──────────────────────────────────┤
│ run_id               │ 123 (points to specific run)     │
│ owner_id             │ 5 (user who created the run)     │
│ geojson              │ Polygon from buffered path       │
│ distance_km          │ 5.2 (how far they ran)           │
│ activity_type        │ 'run' or 'cycle'                 │
│ created_at           │ Timestamp of run completion      │
│ visual_appearance    │ Organic shape (not square grid)  │
│ ownership_model      │ Permanent (never lost)           │
│ uniqueness           │ Different paths = different      │
│                      │ territories (even same location) │
└──────────────────────┴──────────────────────────────────┘
```

## Frontend Rendering Pipeline

```
┌──────────────────────────────┐
│ GET /api/territories?limit=500
└───────────────┬──────────────┘
                │
                ▼
┌─────────────────────────────────────────────┐
│ API Response: Array of Territory objects    │
│ [{id: 1, run_id: 123, owner_id: 5,         │
│   geojson: {Polygon}, distance_km: 5.2}]   │
└───────────────┬─────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────┐
│ RealTerritoryMap.tsx                        │
│ • Map over territories array                │
│ • Extract geojson coordinates               │
│ • Get owner's color                         │
└───────────────┬─────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│ For each territory:                  │
│ <Polygon                             │
│   key={territory.run_id}             │
│   positions={latLngArray}            │
│   pathOptions={{color, fill}}        │
│   onClick={() => {                   │
│     apiFetch(`/territories/         │
│       ${territory.run_id}/info`)    │
│   }}                                 │
│ />                                   │
└───────────────┬──────────────────────┘
                │
                ▼
        ┌───────────────┐
        │  Map Display  │
        │   on Leaflet  │
        │  [Polygons]   │
        └───────────────┘
```

## Territory Info Popup Flow

```
User clicks territory on map
          │
          ▼
handleTerritoryClick(territory.run_id)
          │
          ▼
GET /api/territories/:runId/info
          │
          ▼
┌──────────────────────────────────────┐
│ Territory Data from Backend:         │
│ {                                    │
│   id: 42,                            │
│   run_id: 123,                       │
│   owner_id: 5,                       │
│   owner_name: "jane_doe",            │
│   distance_km: 5.2,                  │
│   activity_type: "run",              │
│   created_at: "2026-01-09T12:00Z"   │
│ }                                    │
└──────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────┐
│ TerritoryInfoPanel.tsx shows:        │
│                                      │
│ Territory Details                    │
│ ─────────────────────────            │
│ Owner: jane_doe                      │
│ Distance: 5.2 km                     │
│ Activity: 🏃 Running                 │
│ Created: Jan 9, 2026                 │
│                                      │
│ "Created from user's 5km run"        │
└──────────────────────────────────────┘
```

## Data Flow Summary

```
                    ┌──────────┐
                    │  USERS   │
                    └────┬─────┘
                         │1
                      1:N│
                         │
        ┌────────────────┴────────────────┐
        │                                 │
        ▼1                               1▼
    ┌──────────┐                    ┌──────────┐
    │   RUNS   │◄────── 1:1 ───────►│TERRITORIES
    └──────────┘    (run_id)         └──────────┘
        │
    contains 50+
    GPS Points
        │
        ▼
    Linestring ──Buffer±50m──> Polygon
                                   │
                            stored in geojson
```

## API Endpoints Summary

```
┌─────┬──────────────────────────┬────────────────────────────┐
│ M   │ Endpoint                 │ Purpose                    │
├─────┼──────────────────────────┼────────────────────────────┤
│ POST│ /api/runs                │ Submit single run          │
│ POST│ /api/runs/sync           │ Batch submit offline runs  │
│ GET │ /api/territories         │ Get all territories       │
│ GET │ /api/territories         │ Filter by ownerId         │
│     │ ?ownerId=5               │                            │
│ GET │ /api/territories         │ Get user's own territories│
│     │ /mine-history            │                            │
│ GET │ /api/territories/:runId  │ Get territory details     │
│     │ /info                    │                            │
└─────┴──────────────────────────┴────────────────────────────┘
```

---

**Created**: January 9, 2026
**System**: Path-Based Territories v1.0
**Status**: ✅ Implementation Complete
